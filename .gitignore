/vendor
composer.lock
composer.phar

.DS_Store
Thumbs.db
tests/.phpunit.result.cache
/tests/integration/tmp
.vagrant
.idea/*
.vscode
js/coverage-ts

# Only English is tracked. Every other language belongs in a community language pack
# (flarum-lang/*), which is where forum owners already look for translations.
locale/*.yml
!locale/en.yml
